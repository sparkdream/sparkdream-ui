import ArchiveSectionRedirect from "@/components/ArchiveSectionRedirect";

export default function SeasonArchivePage() {
  // No season archive route yet; the redirect lands on the snapshot landing.
  return <ArchiveSectionRedirect section="" />;
}
